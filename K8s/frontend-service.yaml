apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend-app
  name: frontend-app-svc
  namespace: default
  selfLink: /api/v1/namespaces/default/services/frontend-app-svc
spec:
  type: LoadBalancer
  selector:
    app: frontend-app
  ports:
    - port: 80
      targetPort: 80